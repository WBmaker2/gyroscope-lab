# 변경 기록

## 개선 — 2026-09-24

- 회전 속도 상한을 240 rad/s로 맞춰 100→200 rad/s의 정확한 2배 비교를 지원한다. 범위 초과 시 값을 자르지 않고 안내한다.
- 변경 범위를 기록하도록 시나리오 버전을 `precession-compare/2`로 올렸다.
- 예측 선택을 시작 조건으로 확인하고, 실행 완료 뒤 비교 단계로 이동하는 링크를 제공한다.
- 용어 풀이를 예측 전에 보여 주고, 계산 모형·엔진 정보는 펼쳐서 보도록 정리했다.
- 실행 중 초점을 정지 버튼으로 옮기고, 상태 문구 중복을 줄였다. 3D 불러오기 완료를 알리며 기록란에는 문장 틀을 제공한다.

## 0.2.0 — 2026-09-23

알려진 빚을 직접 해소하고 공개했다 (https://wbmaker2.github.io/gyroscope-lab/).

- `main.ts`를 9개 모듈로 나눴다. 전부 500줄 이하, 동작은 같다.
- 고대비 모드 대응, 200% 확대 확인, 읽기 순서 확인, 과제 문장을 쉽게 고쳤다.
  (`docs/ACCESSIBILITY-CHECKS.md`)
- Three.js 3D 보기를 붙였다. 같은 계산 상태를 그리고, 카메라는 정면·위·옆만,
  자동 회전 없음. WebGL이 안 되면 2D로 보여준다.
- P1 별도 모델: 약한 감쇠(끄면 P0와 같음), 지면 접촉 감지(닿으면 끝, 닿은 뒤는 계산 안 함).
  엔진 버전 v0.2.0. 실험 영상 비교는 출처 검수 불가로 넣지 않았다.
- 3D는 누를 때만 내려받는다. 성능·교과 검토 문서를 더했다 (`docs/PERF.md`,
  `docs/CURRICULUM-REVIEW.md`).

## 0.1.0 — 2026-09-23 (배포 전)

P0 첫 완성. 아직 공개 주소에 올리지는 않았다.

- 예측→실험→비교→기록 흐름과 2D 투영·수치표·시간 그래프를 만들었다.
- 계산 엔진(RK4, Δt=1/2000초)과 화면을 분리했다.
- 빠른 회전 근사는 비교용으로만 표시한다. 느리게 돌리면 어긋난다고 알린다.
- 기록에 엔진 버전·시나리오 버전을 함께 저장한다.
- 320·360·390·768·1280·1440px에서 가로 넘침 없이 보인다.
- 키보드와 동작 줄이기 설정을 지원한다.
- 시간 그래프(축 높이·에너지) 추가. 기록에 엔진·시나리오 버전 포함.
  실행 없이 기록 저장을 막는다.
