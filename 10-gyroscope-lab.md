# 10. 넘어지지 않는 팽이 연구소

- 프로젝트: `gyroscope-lab`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 10
- 대상: 중·고등학교 물리·수학 / 활동: 25분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문과 차별성

‘회전하는 바퀴에 힘을 가하면 왜 예상과 다른 방향으로 움직일까?’ 각운동량과 토크의 관계를 벡터와 움직임으로 연결한다.
기존 수평잡기의 정적 평형과 달리 시간에 따라 방향이 바뀌는 강체 회전이다.
10순위는 수치 적분·좌표계·특이점 오류가 학습 내용 자체를 왜곡할 수 있어 검증 비용이 크기 때문이다.

## 2. 목표·오개념

- 중력의 토크와 각운동량 방향 변화를 연결한다.
- 빠른 회전에서의 세차 근사와 일반 운동을 구분한다.
- 느린 회전에서는 단순 세차 공식이 맞지 않을 수 있음을 설명한다.
- ‘팽이에는 중력이 작용하지 않는다’, ‘회전하면 항상 안 넘어진다’를 방지한다.

## 3. 흐름

1. 3분: 기울어진 회전 바퀴의 다음 움직임을 예측한다.
2. 5분: 중력·토크·각운동량 벡터를 켜고 한 단계씩 진행한다.
3. 7분: 회전 속도만 2배로 바꿔 세차를 비교한다.
4. 5분: 낮은 회전 속도에서 근사가 어긋나는 사례를 관찰한다.
5. 5분: 같은 힘인데 다른 운동이 나타나는 이유를 기록한다.
- 전이: 무게중심과 지지점 거리를 절반으로 줄이면 빠른 회전 근사에서 무엇이 바뀌는지 예측한다.

## 4. 화면·상태

| 화면 | 표시 | 행동 |
|---|---|---|
| 설정 | 질량·거리·각속도·기울기 | 예측·시작 |
| 관찰 | 팽이·벡터·시간 | 정지·한 단계·시점 복귀 |
| 비교 | 축 방향 궤적·근사값 | 실행 두 개 비교 |
| 기록 | 조건·한계·설명 | 저장 |

ready → running → paused/limitReached/numericalError. 지면 접촉은 P0에서 계산하지 않으며 축이 허용 기울기를 넘으면 종료한다.
수치 실패 시 마지막 유효 상태와 에너지 오차를 보존하고 초기값 복원을 제공한다.

## 5. 물리 모델

- 고정 지지점의 축대칭 강체, 질량 m=1kg, 무게중심 거리 l=0.1m, g=9.81m/s².
- 지지점 기준 관성 텐서 I=diag(I_perp,I_perp,I_axis), 기본 I_perp=0.02,I_axis=0.01 kg·m².
- 내부 상태는 몸체→세계 회전 quaternion q와 세계 각운동량 L.
- 각속도 ω_world=R I^-1 R^T L, 토크 τ=r_world×(0,0,-mg), dL/dt=τ.
- qdot=0.5*[0,ω_world]⊗q 규약을 사용하고 quaternion 라이브러리 순서와 곱 방향을 문서화한다.
- r_world=R(0,0,l). 지지점은 고정되고 접촉 마찰·미끄럼·공기 저항은 P0 제외한다.
- 초기 기울기 5~35°, 축 각속도 20~240 rad/s. 초기 L은 지정한 각속도와 관성 텐서에서 계산한다.
- RK4 고정 Δt=1/2000s 후보. 렌더링과 적분을 분리하고 q는 매 단계 정규화한다.
- 빠른 회전 근사 Ω≈mgl/(I_axis ω_spin)는 비교용이며 모든 조건의 실제 결과라고 쓰지 않는다.
- 예: ω_spin=100rad/s이면 근사 Ω≈0.981rad/s. 초기 조건에 따라 너테이션이 함께 존재할 수 있다.
- 총 에너지 E=0.5ω·L+mg*r_world.z. 무감쇠 모델의 장시간 오차를 모니터링한다.
- P0 실행 시간 10초. 에너지 상대 드리프트 목표 0.5% 이내를 만족하지 못하면 Δt 또는 적분법을 재검토한다.

## 6. 데이터·판정

- Parameters: massKg, comDistanceM, inertiaPivot, tiltRad, spinRadPerSec, dt.
- State: t, quaternion, angularMomentumWorld, omegaWorld, energy, axisDirection.
- Run: parameters, samples, maxEnergyDrift, terminationReason.
- 학생 평가는 속도 변화에 대한 예측과 벡터 설명으로 한다. 너테이션이 보이면 실패로 채점하지 않는다.
- 회전 속도·세차 속도는 모두 rad/s이며 RPM 표시는 변환값으로만 제공한다.

## 7. Three.js·대체 화면

Three.js는 축 방향과 벡터를 실제 계산 상태로 그린다. quaternion 보간은 화면 부드러움에만 사용한다.
카메라 자동 회전은 끄고 정면·위·옆 보기 버튼을 제공한다.
WebGL 실패 시 축 방향의 위/옆 투영과 벡터 수치·시간 그래프를 제공한다.
모션 축소에서는 초기 정지와 단계 이동이 기본이다.

## 8. 생성 이미지

- P0 팽이·바퀴·자세 제어 상황 각 2장=6장. 확장 18장.
- 예시: ‘밝은 과학 교실의 자이로스코프 실험 도입 삽화, 회전 방향 화살표나 수식 없이 장치와 손잡이만, 일관된 조명’.
- 방향 화살표·축·각운동량은 엔진으로 표시한다. 이미지의 힘 방향을 정답 근거로 쓰지 않는다.
- 검수: 실제 계산 장치와 다른 지지 조건을 같은 실험처럼 소개하지 않기.

## 9. 범위·구현 단계

- P0: 고정점 축대칭 강체, 중력, 벡터, 두 실행 비교, 근사값과 수치 결과 구분.
- P1: 약한 감쇠·지면 접촉은 별도 모델로 추가한다. 실험 영상 비교는 출처 검수 후 도입한다.
- 제외: 자전거 전체 안정성, 실제 우주선 제어, 임의 복합체 충돌.
- M1: 좌표 규약·quaternion → M2: 무토크 보존 시험 → M3: 중력·근사 비교 → M4: 3D → M5: 경계·모바일.
- 모듈: rigidBody.ts, quaternionStep.ts, torque.ts, invariants.ts, integratorWorker.ts, GyroScene.tsx, Compare.tsx. 각각 500줄 미만.

## 10. 검증·완료

- g=0이면 세계 L 일정. 구형 관성 텐서이면 ω와 L 평행.
- 축이 수직이고 초기 횡각속도 0이면 중력 토크 0.
- Δt를 절반으로 줄인 1초 결과가 수렴하고 q norm 오차 1e-10 이하 목표.
- 오른손 법칙 방향, 에너지 드리프트, 근사 적용 범위, 느린 회전 종료를 검증한다.
- 키보드 입력·모바일·reduced-motion·2D 투영에서도 비교 과제를 완료한다.
- gi-pulse는 실행/비교 버튼, 업데이트 내역 제공, VoiceOver 제외.
- 완료: 벡터와 운동이 동일 상태를 사용하고 보존 검사·근사 한계가 표시되며 실패를 성공으로 숨기지 않는다.
