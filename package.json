{
  "name": "gyroscope-lab",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "넘어지지 않는 팽이 연구소 — 고정점 축대칭 강체 세차 실험 (P0)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
